name: flutter_provider_pomodorotimer_chart
description: 'A new Flutter project.'
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.0.0
  hive: ^2.0.0
  hive_flutter: ^1.1.0
  fl_chart: ^0.63.0
  path_provider: ^2.0.0
  build_runner: ^2.4.9
  hive_generator: ^2.0.1
  google_fonts: ^6.2.1
  flutter_local_notifications: ^19.3.0

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
